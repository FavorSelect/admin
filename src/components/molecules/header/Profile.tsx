import React from "react";
import { User } from "lucide-react";

const Profile = () => {
  return (
    <div className="bg-gray-100 w-9 h-9 flex justify-center items-center rounded-full cursor-pointer">
      <User />
    </div>
  );
};

export default Profile;
